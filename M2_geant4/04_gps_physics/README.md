# Lesson 4 - GPS e liste di fisica

Questa versione usa `G4GeneralParticleSource`.
Per cambiare physics list, modifica `app/main.cc`.

Esempio:
- `FTFP_BERT()` per una scelta generale
- `FTFP_BERT_EMZ()` se vuoi studiare opzioni EM più raffinate
